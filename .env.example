# Railway injects DATABASE_URL automatically once you add a Postgres plugin
# to this project — you shouldn't need to set it by hand there.
DATABASE_URL=postgres://user:password@localhost:5432/andas_slate
PORT=3000
